version: '3.8'
services:
  db:
    image: postgres:14
    env_file: .env  # Использует переменные из .env
    ports:
      - "5432:5432"  # Проброс порта для локального доступа
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Сохранение данных
volumes:
  postgres_data: